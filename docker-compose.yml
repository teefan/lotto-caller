services:
  web:
    image: nginx:alpine
    container_name: lotto-caller
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html:ro
    networks:
      - caddy_net

networks:
  caddy_net:
    external: true
